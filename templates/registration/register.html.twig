{% extends 'base.html.twig' %}

{% block title %}Register{% endblock %}

{% block body %}
    {% for flashError in app.flashes('verify_email_error') %}
        <div class="alert alert-danger" role="alert">{{ flashError }}</div>
    {% endfor %}

    
    <div class="login-box">
    <h2>Register</h2>
    <p>veuiller remplire le formulaire</p>
     Email:
    {{ form_start(registrationForm, {'attr': {'id': 'registerForm'}}) }}
       <div class="user-box">
        {{ form_row(registrationForm.email) }}
        </div>
        Mot de passe:
        <div class="user-box">
        {{ form_row(registrationForm.plainPassword) }}
        </div>
        {{ form_row(registrationForm.agreeTerms) }}

       

            <a href="#" onclick="document.getElementById('registerForm').submit()">     <span></span>
            <span></span>
            <span></span>
            <span></span>
          Inscription
            </a>

    {{ form_end(registrationForm) }}
      <a class="connect"  href="/login"><P> Si vous avez déjà un compte cliquer Ici</p></a>
    </div>
{% endblock %}
